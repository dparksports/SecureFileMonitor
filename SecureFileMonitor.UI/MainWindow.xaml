<Window x:Class="SecureFileMonitor.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SecureFileMonitor.UI"
        xmlns:vm="clr-namespace:SecureFileMonitor.UI.ViewModels"
        mc:Ignorable="d"
        Title="Secure File Monitor - PROTECTED" Height="650" Width="1000"
        Background="#181818"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="20,15">
            <StackPanel Orientation="Horizontal">
                <!-- Shield Icon placeholder -->
                <TextBlock Text="&#xE8B9;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="SpringGreen" Margin="0,0,10,0"/>
                <TextBlock Text="SECURE FILE MONITOR" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                <Border Background="#33FF0000" CornerRadius="4" Margin="20,0,0,0" Padding="10,2" VerticalAlignment="Center">
                    <TextBlock Text="HIGH SECURITY MODE" Foreground="Red" FontWeight="SemiBold" FontSize="12"/>
                </Border>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="{Binding StatusMessage}" Foreground="LightGray" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <Button Command="{Binding StartMonitoringCommand}" Content="Start" Background="Green" Foreground="White" Padding="15,5" Margin="0,0,10,0" IsEnabled="{Binding IsMonitoring, Converter={StaticResource InverseBooleanConverter}}"/>
                <Button Command="{Binding StopMonitoringCommand}" Content="Stop" Background="Red" Foreground="White" Padding="15,5" IsEnabled="{Binding IsMonitoring}"/>
            </StackPanel>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar Stats -->
            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                <Border Background="#222" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Database" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Encrypted (SQLCipher)" Foreground="Gray"/>
                    </StackPanel>
                </Border>
                <Border Background="#222" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Monitor Status" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="{Binding IsMonitoring, StringFormat='Active: {0}'}" Foreground="SpringGreen"/>
                    </StackPanel>
                </Border>
                <Border Background="#222" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Processing" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Tracking changes..." Foreground="Gray"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Main Content Area with Tabs -->
            <Border Grid.Column="1" Background="#11FFFFFF" CornerRadius="8" Padding="10">
                <TabControl Background="Transparent" BorderThickness="0">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Foreground" Value="Gray"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TabItem">
                                        <Border Name="Border" BorderThickness="0,0,0,2" BorderBrush="Transparent" Padding="10,5">
                                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="SpringGreen"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TabControl.Resources>
                    
                    <TabItem Header="LIVE ACTIVITY">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="REAL-TIME EVENTS" FontWeight="SemiBold" Foreground="LightGray" Margin="0,10,0,10"/>
                            
                            <DataGrid Grid.Row="1" ItemsSource="{Binding RecentActivity}" AutoGenerateColumns="False" 
                                      Background="Transparent" BorderThickness="0" HeadersVisibility="Column"
                                      IsReadOnly="True" RowBackground="#222" AlternatingRowBackground="#2A2A2A" Foreground="White">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat=HH:mm:ss}" Width="80"/>
                                    <DataGridTextColumn Header="Operation" Binding="{Binding Operation}" Width="80"/>
                                    <DataGridTextColumn Header="Process" Binding="{Binding ProcessName}" Width="120"/>
                                    <DataGridTextColumn Header="User" Binding="{Binding UserName}" Width="100"/>
                                    <DataGridTextColumn Header="Path" Binding="{Binding FilePath}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </TabItem>
                    
                    <TabItem Header="DUPLICATES">
                         <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,10">
                                <TextBlock Text="DUPLICATE FILE FINDER" FontWeight="SemiBold" Foreground="LightGray" VerticalAlignment="Center" Margin="0,0,20,0"/>
                                <Button Command="{Binding ScanDuplicatesCommand}" Content="Scan Now" Background="#333" Foreground="White" Padding="10,2"/>
                            </StackPanel>
                            
                            <DataGrid Grid.Row="1" ItemsSource="{Binding DuplicateFiles}" AutoGenerateColumns="False" 
                                      Background="Transparent" BorderThickness="0" HeadersVisibility="Column"
                                      IsReadOnly="True" RowBackground="#222" AlternatingRowBackground="#2A2A2A" Foreground="White">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Hash" Binding="{Binding CurrentHash}" Width="200"/>
                                    <DataGridTextColumn Header="Path" Binding="{Binding FilePath}" Width="*"/>
                                    <DataGridTextColumn Header="Size" Binding="{Binding FileId}" Width="80"/> <!-- Placeholder for Size if added later -->
                                    <DataGridTextColumn Header="Last Modified" Binding="{Binding LastModified}" Width="150"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Border>
        </Grid>

        <!-- Footer -->
        <StatusBar Grid.Row="2" Background="#111111" Foreground="Gray">
            <StatusBarItem>
                <TextBlock Text="System Integrity: OK"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="SecureFileMonitor v1.0.0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
